# Review 2026-05-02

Scope: uncommitted working tree diff for branch `11-add-robust-opencode-orchestration-harness-with-model-settings-and-orchestration-skills`.

## Findings

1. Medium: profile-mode worker starts and `list_worker_profiles` validate profiles against `createWorkerCapabilityCatalog()` without daemon/backend diagnostics. Because missing status becomes `not_checked` and is treated as available, a profile can be reported as valid and accepted by `start_run` even when the referenced worker CLI is missing or unsupported. This weakens the live profile gate described in the plan and docs. Evidence: `src/orchestratorService.ts:181`, `src/orchestratorService.ts:250`, `src/opencode/capabilities.ts:130`.

2. Low: `docs/ai-workspace.md` contradicts the implemented shared skill-root model. It says orchestration workflows are not stored in `.agents/skills/`, then immediately says they live under `.agents/skills/orchestrate-{name}/SKILL.md`. Evidence: `docs/ai-workspace.md:42`.

3. Low: README environment fallback docs omit the new `AGENT_ORCHESTRATOR_OPENCODE_PROFILES_FILE` and `AGENT_ORCHESTRATOR_OPENCODE_PROFILES_JSON` names even though the launcher help and parser support them. Users reading README see only deprecated route names. Evidence: `README.md:233`, `src/opencode/launcher.ts:43`, `src/opencode/launcher.ts:212`.

## Test Gaps

- No focused test covers profile-mode `start_run` or `list_worker_profiles` with a missing or unsupported backend diagnostic.
- No docs/projection check was run during this review.
